#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to format and lint staged files
npx lint-staged

# Run type checking
npm run typecheck

# Check for any build issues with shared types
npm run build:types